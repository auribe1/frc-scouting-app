# FRC Scouting App (WIP)

Offline-first scouting project for FRC matches.

## Current features
- ScoutingEntry data model (UUID + timestamp)
- Local storage (JSONL)
- Defense rating (1–5) validation

## Run
```bash
python main.py
